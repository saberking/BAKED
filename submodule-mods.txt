Submodule dpf contains modified content
diff --git a/dpf/distrho/DistrhoPlugin.hpp b/dpf/distrho/DistrhoPlugin.hpp
index 8570f9e9..79d1197e 100644
--- a/dpf/distrho/DistrhoPlugin.hpp
+++ b/dpf/distrho/DistrhoPlugin.hpp
@@ -60,6 +60,7 @@ START_NAMESPACE_DISTRHO
 class Plugin
 {
 public:
+    void *host;
    /**
       Plugin class constructor.@n
       You must set all parameter values to their defaults, matching ParameterRanges::def.
diff --git a/dpf/distrho/src/DistrhoPluginCLAP.cpp b/dpf/distrho/src/DistrhoPluginCLAP.cpp
index 59390921..e93aaa11 100644
--- a/dpf/distrho/src/DistrhoPluginCLAP.cpp
+++ b/dpf/distrho/src/DistrhoPluginCLAP.cpp
@@ -67,10 +67,14 @@
 # define DPF_CLAP_TIMER_INTERVAL 16 /* ~60 fps */
 #endif
 
+
 START_NAMESPACE_DISTRHO
 
 // --------------------------------------------------------------------------------------------------------------------
 
+
+
+
 typedef std::map<const String, String> StringMap;
 
 struct ClapEventQueue
@@ -199,6 +203,8 @@ static constexpr const sendNoteFunc sendNoteCallback = nullptr;
 class ClapUI : public DGL_NAMESPACE::IdleCallback
 {
 public:
+    const void* clapPointer = NULL;
+
     ClapUI(PluginExporter& plugin,
            ClapEventQueue* const eventQueue,
            const clap_host_t* const host,
@@ -232,6 +238,7 @@ public:
           fParentWindow(0),
           fTransientWindow(0)
     {
+        clapPointer=host;
        #if DISTRHO_PLUGIN_WANT_MIDI_INPUT
         fNotesRingBuffer.setRingBuffer(&eventQueue->fNotesBuffer, false);
        #endif
@@ -628,6 +635,8 @@ private:
             if (fTransientWindow != 0)
                 fUI->setWindowTransientWinId(fTransientWindow);
         }
+        Plugin * foo = (Plugin *)fPlugin.getInstancePointer();
+        foo->host=(void *)clapPointer;
     }
 
     // ----------------------------------------------------------------------------------------------------------------
@@ -747,6 +756,7 @@ static constexpr const updateStateValueFunc updateStateValueCallback = nullptr;
 class PluginCLAP : ClapEventQueue
 {
 public:
+
     PluginCLAP(const clap_host_t* const host)
         : fPlugin(this,
                   writeMidiCallback,
